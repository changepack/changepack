section data-test-id="new_post_page"
  = render I::Content.new do |content|
    - content.article do
      = render 'form', post:, commits:

    - content.sidebar do
      .hidden.md:block
        = render I::Metadata.new(post:)
