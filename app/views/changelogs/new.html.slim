.w-2/3 data-test-id="new_page"
  .flex.justify-between.items-center
      section
        h1.font-bold.text-4xl New changelog

  = component :content do |component|
    - component.article do
      = render 'form', changelog: @changelog

    - component.metadata do
      span.mt-1.text-gray-400 = l Date.current, format: :long
